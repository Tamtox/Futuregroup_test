import { Box } from '@mui/material';
import type { IBook } from '@/types/interfaces';
import type { RootState } from '@/store/store';

const Book = (): JSX.Element => {
  return <Box className="book"></Box>;
};

export default Book;
